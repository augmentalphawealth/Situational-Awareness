name: EMA Active Leader Swing Backtest v2

on:
  workflow_dispatch:
    inputs:
      round_trip_cost_bps:
        description: "Round-trip trading cost assumption (basis points)"
        required: false
        default: "20"
      oos_years:
        description: "Years reserved for out-of-sample validation"
        required: false
        default: "2"

permissions:
  contents: read

jobs:
  backtest:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    env:
      ROUND_TRIP_COST_BPS: ${{ github.event.inputs.round_trip_cost_bps || '20' }}
      OOS_YEARS: ${{ github.event.inputs.oos_years || '2' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas numpy pyarrow

      - name: Verify historical database
        run: |
          ls -lh nse_6yr_historical.parquet
          test -f nse_6yr_historical.parquet

      - name: Run Active Leader swing backtest v2
        run: python swing_trend_backtest_v2.py

      - name: Upload backtest outputs
        uses: actions/upload-artifact@v4
        with:
          name: ema-active-leader-backtest-v2
          if-no-files-found: error
          retention-days: 14
          path: |
            active_leader_days.csv
            active_leader_setups.csv
            active_leader_trades.csv
            active_leader_strategy_comparison.csv
            active_leader_yearly.csv
            active_leader_symbol_results.csv
            active_leader_parameter_stability.csv
            active_leader_recommended_candidates.csv
            active_leader_notes.txt
